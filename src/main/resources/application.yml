
spring:
  application:
    name: account-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/account_service
      #uri: mongodb+srv://useris:ispvispv@cluster0.iceaw.mongodb.net/test?authSource=admin&replicaSet=atlas-b109it-shard-0&readPreference=primary&appname=MongoDB%20Compass%20Community&ssl=false
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    status-page-url-path: /swagger-ui/

server:
  port: 5100
  #port: ${PORT:0}

config:
  endpoint:
    enterprise-customers: lb://customer-service/api/enterprise-customers

path:
  accounts: /api/accounts